# E2Eテスト現在地（2026-07-13朝時点）— 別マシン引き継ぎ用

## 別マシンでのセットアップ

```bash
cd rincle-test
npm install
npx playwright install chromium chromium-headless-shell
cp .env.example .env   # → 実値を記入（.envはコミットされない。値は前マシンから手動転送）
npx playwright test rincle.e2e.ts
```

手動で持っていくもの（gitに入っていない）:
- `rincle-test/.env`（認証情報・予約日時）
- `documents/rincle.bubble`（新構築エクスポート・APIキー入りのためignore。DB/実装解析を続ける場合のみ必要。Bubbleエディタから再エクスポートでも可）
- `.context/rincle.bubble`（旧本番エクスポート。同上）

## スイートの状態（rincle.e2e.ts / 対象: https://rincle.co.jp/version-13fge）

- **17テスト中: 15 pass / 1 skip（正当理由）/ 1 fail（アプリ側障害の正検出）**
- 改修の経緯・新アプリの構造知識は `E2E_PLAN_20260712.md` とテスト内コメント参照
- skip: テスト4「料金ページ」— 旧`/index/howtopay`は新アプリで機能ごと削除済み（要確認事項）
- **fail: テスト7「予約フロー」— アプリ側のデグレを検出中（テストは正しい）**。詳細下記

## 🔴 検出中のアプリ障害（山本さんへ報告要・7/13リリース判断に直結）

**bicycle_detailで日時選択→「予約画面へ進む」を押してもワークフローが発火しない**（予約が作られず遷移しない）。

証拠（2026-07-13朝・再現性あり）:
- クリック後30秒監視してもBubbleワークフローのHTTP呼び出しがゼロ（ネットワーク監視で確認）
- コンソールに未捕捉ReferenceError: **`bubble_fn_result_date is not defined`**（bicycle_detailの日付計算Run JS）、**`bubble_fn_visible_count is not defined`**（自転車一覧）。ToolboxのJavascriptToBubble受け口要素が実行時に初期化されていない
- **7/12夜の時点では同フローで予約確定の確認ポップアップまで到達できていた** → その後の変更でデグレした可能性大
- 副作用: WF不発火のため残骸予約は作られていない

## アプリ修正後にやること（テスト7の続き）

1. `npx playwright test rincle.e2e.ts --grep "予約フロー|予約一覧確認|予約キャンセル"` を2回連続で実行し安定を確認
   - テスト7は「予約が確定しました」ポップアップ＋予約一覧への反映＋予約番号取得まで厳格にassertする（店頭決済選択・カード入力検出時は安全停止のガード付き）
   - テスト9が同日付の予約を全キャンセルして後始末する
2. 予約確定時の**メール実受信確認**（ユーザー/店舗/管理者3通。#12レポートのD-1が未修正なら管理者通知はユーザー宛に誤送されるはず — それ自体が検証項目）
3. **SendGrid到達性（E-1）**: Gmail/Outlookで迷惑メール判定されないか確認
4. その他リリース前に報告済みの検出事項: `07_release/email-url-check_20260712.xlsx` ①指摘一覧、E2E中の新規発見（お問い合わせ種別に「管理者テスト」等のテスト選択肢残存、howtopay消滅、faq/guide転倒、ヘッダーログインボタンのmode=signinバグ、トップのアンカーURLに全角＃、備考欄の¥145,650重複表示）

## 運用メモ

- Bubbleは`networkidle`が確定しないことがある → login()は`domcontentloaded`+要素待ちに変更済み。新規の待機も同方式を推奨
- `/user_reservation_list`へ直接gotoするとトップにバウンス → トップの「予約の確認・キャンセル」ボタン経由で開く
- アプリ更新バナー（開発中は頻発）でWFが凍結する → `freshenIfStale()`参照
- probe_*.js / inspect_*.js は調査用ワンオフ（テスト実行には不要）
