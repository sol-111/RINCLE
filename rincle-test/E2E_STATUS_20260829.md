# E2E改修結果（2026-08-29）— 最新エクスポート対応・全42本グリーン

8/29エクスポートの `documents/rincle.bubble` で地図を作り直し（`rincle-test/map/`・gitignore対象）、
現行4スイートを最新アプリ（**version-test**）に合わせて差分更新した。

## 結果

**42本中 41 pass / 1 skip（料金ページ＝旧アプリ機能のため意図的skip）。fail 0。**
7/16時点で両環境共通だったfail 10件はすべて解消（アプリ側のバグではなく、下記のUI刷新+フィクスチャ再作成に
テストが追従できていなかったのが実態。アプリの不具合として報告すべきものは今回は見つからなかった）。

## 接続先の変更

- 旧デフォルトの `version-43erq` は**404（消滅）**。現在の実行先は `version-test`
- 実行方法: `RINCLE_BASE_URL="https://rincle.co.jp/version-test" RINCLE_DATE="2026/09/05 11:00" RINCLE_TIME="2026/09/05 19:00" npx playwright test rincle.e2e.ts rincle-store.e2e.ts rincle-admin.e2e.ts rincle-integration.e2e.ts`
  （日付は「テスト店舗の休業日=火・水」を避けた将来日にする）
- コード内のデフォルトBASE_URLは43erqのまま（環境変数で上書きする運用）。次に恒久ブランチが決まったら差し替え

## アプリのUI刷新（7月中旬以降）とテストの追従内容

1. **検索結果がタブ切替式に**: 「店舗一覧/自転車一覧」タブ（URLは `mode=shop|bicycle`）。
   旧「貸出可能な自転車をすべて見る」ボタンは廃止 → 全テストをタブ切替+「詳細を見る」に変更
2. **予約一覧が `/mypage` 配下に移動**（旧 `/user_reservation_list`。見出し「予約状況一覧」は同じ）
3. **予約一覧がアコーディオン式に**: 見出し（予約番号・XXXX）クリックで1件だけ展開。
   - 見出しの素クリックは効かない → `.clickable-element` 祖先のjQueryハンドラを直接呼ぶ
   - **キャンセル済み予約もラベル付きで一覧に残る** → 対象探しから除外しないと後始末確認が誤爆
4. **加盟店一覧が116件+キーワード/並び替えUI付きに** → 「古い順」ソート（最古=テスト店舗）で確認
5. **TOPICSの中身は運営が随時入れ替える** → 固定文言をやめ、先頭トピックを動的に選ぶ方式に
6. **別ロールでログイン中はユーザー向けページの一部部品（カレンダー・オプション欄）が描画されない**
   → 店舗→ユーザーの統合確認は `browser.newContext()` の未ログイン別コンテキストで実施

## テストフィクスチャの再作成（7/16-17・共有DB）

- テスト店舗: **SEINO自転車（栃木）→「RINCLE 千葉柏店/オンザロード柏店（レンタル車体準備中）」（千葉・火水休業）**
  登録日2026-07-16。加盟店一覧の「古い順」先頭
- 固定自転車ID: `1784370816467x497983313811946050`（ちゃんとしたロードバイク）。旧IDは読み込み不能
- 都道府県コード: 栃木=9 → 千葉=12（`STORE_PREF_CODE` で上書き可）
- タイプフィルタ検証銘柄: クロスバイク=「金曜日例外日」/ ロードバイク=「ちゃんとしたロードバイク」
  （`TEST_CROSS_BIKE_NAME` / `TEST_ROAD_BIKE_NAME` で上書き可）

## 残っているデータ（要DB掃除・実害なし）

version-test のテストユーザー（shoki.seino）の予約一覧に以下が残存:

- 予約番号 20260724120606（7/25貸出・キャンセル期限切れでUIから消せない）
- 予約番号が**空**のカード1件（仮予約の壊れレコードの疑い。展開不可）

今回のrun残骸（9/5貸出×2件）はUI操作でキャンセル済み。以後のrunは各テストが自動で後始末する。

## 教訓（bubble-e2eスキルの quirks 18・19 に還元済み）

- ロール混在セッションでのユーザー側確認は禁物（静かに描画が欠ける）
- アコーディオン一覧は「素クリック不可・展開1件のみ・削除済みも残る」の3点セットに注意
