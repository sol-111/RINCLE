# 設計判断の記録（decisions）

SKILL.mdの規約が「なぜそうなったか」の経緯と教訓。**普段の資料作りでは読まなくてよい**。規約を変えたくなった時・判断に迷った時に、同じ議論を繰り返さないために読む。新しい判断を下したらここに追記する（新しい日付を上に）。

## 2026-09-25 — 見た目を2点だけコンサル型に寄せる（表の軸・囲みの箱）。全面の作り替えはしない

- 清野さんの問い「エディトリアルをやめてコンサルチックに作り替える？」に対し、作り替えない判断。コンサルっぽさの正体は主張の見出し・軸のある表・因果の三角・全体マップの往復で、今日取り込み済み。明朝と焦がし色の識別性と27ページ+月次レポートの資産を捨てる理由がない
- 寄せたのは2点: ①**表の見出し行を本文と同じ大きさ・太字・墨**にし、**行見出しの軸列に縦罫**を入れる（薄い小文字の見出しは「ここが軸」と読めなかった）②**カード類の囲みをやめて上罫1本**（`.card`=墨・`.compare-box`/`.option`/`.sd-vs-box`=役割色）。「塗り・囲みは本当に立てたい要素だけ」の規約に既存部品を合わせた
- 波及: design.css は同期済み。月次レポート（`seo-report/reports/*.html`）はCSSを生成時にインラインで埋めるので、次回生成から新しい見た目になる（過去分は再生成しない）
- galleryは**文字が上・部品が下**の1列に変更（清野さん提案）。左右2列だと説明が長い部品で縦に間延びし、幅の要る表・図が狭かった。11章「コンサル型の部品」は性格ごとに06表・07チャート・08資料部品・09図解・10スライドへ振り分けた（外出しの章は「新しいから」という分類で、読者の探し方に合わない）

## 2026-09-25 — 内容レビュー1ラリー目の結果を規約に。footerはクライアント向けなら更新日

- 補助金の4本に `review/content-prompt.md` で1ラリー回した（230件・採否表は `documents/98_presentations/review/2026-09-25-content/採否表.md`）。4本に共通して出た14パターン（A1〜A14）のうち12を採用、2を清野さんに判断してもらった
- **A1 「当社」は初出で「当社（株式会社アイビス）」と社名を明記**（清野さん決定）。読者（PEDAL社）が「当社」を自社と誤読する
- **A13 クライアント向けHTMLのfooterは「最終更新 YYYY年M月D日」だけ**（清野さん決定）。正本パスはクライアントに無意味なのでHTMLコメントへ。社内向けは従来どおり「正本: パス」。原則5を改定し、lintはどちらかの形でなければNG
- **lintにWARNの段階を新設**: 内容の規約は機械で確実に判定できないものが多い（評価語に根拠があるか、専門語が説明済みか）。NGにすると偽陽性で手が止まるので、`WARN `接頭辞の指摘は表示だけしてexit codeに数えない。NGにしたのは機械で確実な3つ（章参照が章数を超える・曜日が暦と違う・footerの形）
- **語彙リストはlint.pyの定数**（COLLOQUIAL / JARGON / EVALUATIVE / VARIANT_PAIRS）。案件で出た語を足していく運用。consulting-pptx-skillの `ai-smell-lexicon.md` と同じ思想だが、向こうの語彙集をそのまま持ち込まず、実際にレビューで出た語から始める
- 曜日照合の年は「直前に『20xx年』があればそれ、無ければ本文初出の年」。最初は本文初出の年だけで判定し、2027年の日付（実績報告後の保管期限）を誤検知した
- 不採用にした指摘で記録すべきもの: 「拒否すると交付決定取消は条文にない」→要領13（3）に明記されておりレビュアーの読み落とし（引用を括弧まで載せ直す）。「11の注意点の表を解体」→要領と照合する索引として残し各行に「→ N章」を付ける

## 2026-09-25 — コンサル型の62型を「全部」取り込む。誌面の部品として実装し、デッキと共用

- 最初のデッキ対応で取り込んだのは62型のうち6型だけだった。清野さんの「これだけ？もっと取り入れられるものなかった？」→「全部入れて欲しい。デザインシステムにも取り込む予定？」で方針転換。**見た目が違うから取り込まない、は判断が広すぎた**。型（情報の構造）と見た目（ネイビー・角丸）は別で、型は誌面様式に載せ替えれば使える
- **実装はデッキ専用でなく誌面の部品**にした（template.css・em基準）。同じHTMLが誌面の章にもデッキの1枚にも置ける。デッキ専用は全体マップ・目次・裏表紙・タグチップ・出典の定位置の5つだけ
- **既存の部品で足りる型は作らない**（同じ情報に2つの部品を作らない）: KPI一覧=`.stat-row`・大型数値の対比=`.sd-nums`・現状と目指す姿=`.compare-box.asis/.tobe`・分岐と判断=`.options`・選択肢の比較表=2段ヘッダーの`table`・ロードマップ=`.gantt`・積み上げ棒=`.stackbar`・順位棒=`.hbar`
- **見送った型**: 散布図・小図並列・進捗バブル・比例円・増減縦棒+合計・シナリオ線・計算ロジックの流れ・4列ロードマップ（用途が薄い。使う案件が出たら足す）
- **塗りの読み替え**: 向こうの「並列カード2×2（塗り）」「柱＋土台の墨帯」「主張パネル（墨ベタ）」は、この様式の「塗りは意味がある時だけ・濃い塗りはまとめ帯とコード面だけ」に合わせて白面+罫線・淡い面+焦がしの左罫にした。対向シェブロンの札と効果の幅の棒だけは焦がし/墨の塗り（それ自体が図の主役なので）
- **構造の規約を足した**: 10枚超は全体マップ+タグチップ、前提は左・帰結は右、因果があるときだけ三角、カード群より軸のある表、矢羽の下に積まない、出典は左下、問題→解消を対で、最後は裏表紙。lintで機械判定できるのは全体マップの有無と最終枚が裏表紙かだけ
- 実装上の学び: `.map2`の中央三角は`grid-row:2 / span 40`で全行にまたがせる（`2 / -1`は暗黙グリッドでは効かない）。矢羽の輪郭は`clip-path`が罫線を切るので外層=罫線色+`::before`=白面の2層。`.cycle`は`sin()/cos()`で等間隔配置。ハーベイボールは`conic-gradient`。ガリーの見本ページを正規表現で切り出すとネストしたdivで壊れるので、確認用の切り出しは`.section`単位で行う

## 2026-09-25 — デッキ（投影用スライド）に対応。様式は誌面のまま

- 清野さんの依頼「スライド形式にも対応できるようにスキル更新できる？」。gallery.htmlのスライド7型は560px見本のpx値で、そのまま投影すると崩れるので**em基準のデッキ用エンジン**を`template.css`の`.deck`に足した（1em=スライド幅/60。幅1440pxで24px・本文`.sd-body`は.82em≒18〜20px）。parts.cssに置いていた`.sd-*`は「UI部品でない」のでtemplate.cssへ移動
- **見た目は誌面様式のまま**（清野さんの判断）。consulting-pptx-skill流のネイビー・角丸禁止・下部結論帯は取り込まない。**表5行・箇条書き5本・本文18px以上**も清野さんの判断で固定
- 型は7→12種。足したのは「本文の基本形（見出し帯+本文）」「2カラム」「前提→帰結」「矢羽」「軸のある表」「図解1枚」。表と図は新しい部品を作らず**誌面の`table`と図の部品をそのまま置く**（デッキ専用の表を作ると二重管理になる）
- 器`deck.html`: 1 section=1枚、矢印/スペース/クリックで送り、ハッシュ直リンク、`.pg`にページ番号を自動記入。印刷は`@page 338.67mm×190.5mm`で1枚1ページ（Chrome headlessでPDF化を確認。12枚→12ページ・960×540pt）。**PPTX変換はしない** — HTMLとPDFが納品形
- lintは`lint-mode: deck`/`body.deck`で分岐し、誌面の規約（ヒーロー・章ナビ・01章）を外してデッキの規約（見出し1つ・48字・常体・表5行・箇条書き5本・下枠とページ番号・小さい文字の直書き）をみる。文章規約と敬語は`text_rules()`に切り出して両方で共用
- 見出しの規約は誌面の原則13と同じ（主張1文・個数を書かない・「ラベル：」にしない）。**デッキでは常体**にした — 投影して口で説明する前提で、です・ますは冗長

## 2026-09-25 — 章副題は主張文に。内容レビューを工程に追加（コンサル型への寄せ）

- carnot-tech/consulting-pptx-skill（MIT）を読み比べた。見た目（16:9・ネイビー・角丸禁止・62型）は方向が違うので取り込まない。取り込んだのは**メッセージと論理の規律**と**内容レビューの工程**。「コンサルっぽさ」の正体は見た目ではなく、見出しが主張になっていて、章ごとに1メッセージで、通し読みで1本の話になることだった
- **02章以降の`sh-sub`を「その章の主張1文」に変更**（清野さんの判断）。2026-09-14の「sh-sub=何を扱うか」は**01章だけの例外**として残す（結論本文の1文目と同文になるのを避けるため）。lintは01章の同文チェックのみ継続し、02章以降が主張文かは目視（機械では判定できない）
- **原則13を新設**: 見出しに個数を書かない／「ラベル：主張」型にしない／評価語には同じ章に根拠／事実より強く言い切らない／主語述語を落とさない／当たり前を箇条書きにしない／切り口を変える／1資料1用語
- **`review/content-prompt.md` を新設**: 文脈を持たない別エージェントにHTMLを読ませて日本語・論理・破綻を拾う。デザインの初見レビューと違い撮影不要で毎回回せる。採否表→採用分修正→lint再実行を1ラリーとし、回数は着手時に決める
- 進め方は「規約を先に書く」ではなく「実物で出た指摘から書く」。まず補助金の4本に1ラリー回して、出た指摘を規約に1行ずつ足す（向こうのslide-rules.mdと同じ蓄積の仕方）
- 未着手（次の候補）: `references/ai-smell-lexicon.md`の持ち込み、lintへのAI臭語・表記ゆれ・見出しの個数表現のWARN、lintが「h2＋sh-subの一覧」を出力して通し読みを助ける機能

## 2026-09-24 — 差分表 `.dtable` を部品に昇格

- 職務経歴書の改訂レビュー（`07_職務経歴書/html/比較_*.html`・生成は`_build_compare.py`）で、正本mdの「現行版」と「修正案」を行単位で左右に並べ、直す・直さないを決めてもらう資料が3ページ分できていた。ページ固有CSSに留まっていたものを5点セットで昇格（template.css / snippets.html / gallery.html / SKILL.md / lint.py）。決めたこと:
  - **色は新しく作らず alert の意味色を使う**: 消した字=紅`<del>` / 足した字=緑`<ins>`。取り消し線は消す（下地の色で足りる。線は引用文の可読性を落とす）。行の左端3pxの罫で「どちら側が動いたか」を示し、`.rep`（置換）は左紅・右緑、`.del`は左紅だけ、`.ins`は右緑だけ
  - **変更のない行は文字色をグレーに落として沈める**（原則3の「opacityで沈めない」と同じ）。続く箇所は`.dfold`の1行に畳み、畳んだ行数を書く（「何行飛ばしたか」が見えないと読者は抜けを疑う）
  - **`del` / `ins` の装飾は`.dtable`の中だけに効かせる**。元の資料では素の要素セレクタで全体にかけていたが、共有CSSに入れると本文の`<del>`にまで波及するため、昇格時にスコープを絞った（改善点1）
  - **凡例は丸の`.legend`を流用せず、罫と同じ3pxの線`.dlegend`で示す**。丸は図解の役割色の語彙。凡例と色の表示トグル（`body.nohl`）は読み方ボックスの中にページで1枚 — 差分表が章ごとに複数並ぶページで、表ごとに凡例を繰り返すとうるさい（図解の「同じ役割構成の図が続く章は先頭に1枚」と同じ）
  - **規模の数字（文字数・増減）は`.dmeta`（明朝22px・ラベルが上）**。答えの数字ではないので`.stat`の特大にしない。数字の序列は `.stat` 60 > `.n.md` 30 > `.dmeta` 22 の3段になった
  - **図ではなく表の族**なので`<figure>`で包まない。lintのFIG_PARTSにも入れない
  - **紙面幅の例外を1つ認めた**: 2段組の引用文で1080pxでは折り返しが多すぎるため、差分表を主役にするページは`.hero-inner,.flow,.flow-tabs,.container,footer`のmax-widthを1320pxに揃えて広げてよい。部品個別のmax-widthではなく紙面そのものの変更なので、原則3の「部品にmax-widthを付けない」とは別物として扱う
  - lintは機械で判定できる4点だけ: `.drow`の種別が4語のどれか1つ / `.dcell`が`<pre>`を持つ / `.dlegend`がページで1枚 / `#hl`があれば`nohl`を切り替えるscriptがある
  - 元の資料にあった`table.summary td.ok/.warn`（★◇の採否列）と`.chip`は昇格しない — 採否は既存の`.badge.good/.mid/.muted`、番号は`.num-chip.sm`で足りる

## 2026-09-16 — 週割り工程表 `.gantt` を部品に昇格

- RINCLE補助金事業のロードマップ（`documents/98_presentations/subsidy/roadmap.html`）で、「誰が・いつ・何を」を1列=1週間の格子で見せる工程表が必要になった。既存の`.phases`（フェーズ帯）は順番しか語れず、**並走（PEDAL側のデザイン制作と当社の設計が同じ週に走る）と、期限の内側・外側（納品後の工程は補助金の期限の外）**を1枚で見せられなかった
- ページ固有CSSで作って描画確認したのち、5点セットで昇格（template.css / snippets.html / gallery.html / SKILL.md / lint.pyのFIG_PARTS）。決めたこと:
  - **棒の色は図解の役割色をそのまま使う**: 書き手側=焦がし`.own` / 相手方=緑`.party` / 共同=墨`.joint`。新しい色を増やさない。期限の外は破線`.after`、毎週の定例など「継続的な関与」は白面+緑輪郭`.hollow`
  - **日付は「月.日」表記**（9.25・11.18）。半角/は文章規約で前後スペースが要り、格子の中では成立しないため。読み方ボックスで表記を断る
  - **棒の中の文言は短く**（overflow:hidden で切れる）。最初の版は「方針決定 → 事務局確認 → 契約・発注」等を詰め込んで全部切れた。詳しい日付は左の工程名の下の`<small>`へ、工程の中身は本文の表へ
  - 左の工程名は「点+名前」を1行目、`<small>`を2行目に置くgrid（flexだと名前が点の横で折れて崩れた）
  - 週数は`--g-weeks`のCSS変数（既定13）。狭い幅は`.gantt-wrap`の横スクロールで受ける（min-width 880px）

## 2026-09-14 — 検収レビュー109件から: 本体の不具合12件と規約14件

9月4日のスキル改定（+4139行）を、**スキルを初めて見るエージェント4本**に検収させた（記録: `documents/98_presentations/review/2026-09-14/`）。1本はスキル本体の技術レビュー（5点セットのクラス名突合・lintへ自作テストHTML 30本・Playwright描画・shell.htmlの操作）、3本は資料23ページの初見レビュー。指摘は109件。

**本体の不具合で重かったもの**

- **lintの旧配色チェックが実質死んでいた**。template.cssの色を自動で許可する仕組みが、**コメントを落とさずに**読んでいたため、ヘッダコメントの「RINCLE: #F95320 → #B8532F」に含まれる旧ブランド原色まで許可していた。11色のうち生きていたのは1色だけ。`<p style="color:#F95320">` を置いたページが ALL CLEAN で通っていた。opacityの許可リスト側には同じコメント除去が入っていたので、**同じ作りを2箇所に書いて片方だけ直した**形の抜け
- **図の主張チェックが、SKILL.mdの推奨どおりの組み方を誤検知していた**。「直前1200文字に`<figure>`があるか」で判定していたため、47タイルのタイルマップの後ろに横棒を併置する（原則10が名指しで推奨する組み合わせ）と、後ろの横棒から figure が見えずNGになった。`<figure>`/`</figure>`の出現を走査して「いま figure の中か」を状態で持つ形に直し、`break`もやめて図ごとに指摘するようにした
- **存在しない変数`var(--sans)`を参照していた**。`:root`に定義がないので font-family が継承に落ち、横棒ペアとファネルの単位が明朝で出ていた。`td .td-sub`は`'Noto Sans JP',sans-serif`と直書きしてあり、書き手も`--sans`が無いことに気づいていなかった
- **SPAシェルの「戻る」でシェルの外に出ていた**。ページ切替を`history.replaceState`でやっていたので履歴が増えず、末尾の`hashchange`リスナは一度も発火しなかった。ユーザー操作は`pushState`・ハッシュ復元だけ`replaceState`に分けた。あわせて、ようこそ画面`#welcome`が`#content`の子だったため1ページ開いた時点で`innerHTML=''`に巻き込まれて消えていたのを、`#content`の外に出して`hidden`で出し入れする形にした
- **撮影スクリプトに案件固有のハッシュ`#stripe-payment-flow`が埋まっていた**。他案件で回すとハッシュが解決されず「ようこそ画面のまま」が撮れる（エラーも出ない）。第5引数に追い出した。index.htmlが無い案件で未処理例外で落ちていたのも直した
- **タイルマップの6段階の色がRINCLEの焦がし色からの生ハードコード**だった。原則2「色を差し替えるのは`:root`だけ」に反し、別案件でブランド色を替えてもタイルマップだけオレンジのまま残る。`color-mix(in srgb, var(--accent) N%, var(--paper))`でトークンから導出する形にした

**教訓: 同じ仕組みを2箇所に書いたら、直す時も2箇所を見る**。コメント除去も、`.stat`の全変種対応も、`alert`のclass後ろに属性が続く場合も、**docstringには正しく書いてあって実装だけが古い**という形で抜けていた。docstringを規約の正本と宣言している以上、docstringを直したら実装も同じコミットで直す。

**規約は14件変えた**（詳細はSKILL.md）。中心は「機械的に適用すると壊れるもの」の言語化:

- **凡例はその図に出る役割だけ**にする。同じ役割構成の図が続く章は先頭の図の直下に1枚
- **3段階の格付けの重さの順**を決めた: 高=`.badge.brand`（焦がしベタ）/ 中=**新設`.badge.mid`**（白面・焦がし1px輪郭）/ 低=`.badge.plain`（罫線のみ）。`.badge.ink`（黒ベタ）はページ内で唯一の最重要マークにだけ
- **章副題（sh-sub）と結論本文を同文にしない**。sh-sub=この章が何を扱うか / 本文=答え
- **章番号は常に1始まり**・**小見出しの番号はハイフン式**（「4-1」。ドット式「7.1」は使わない）・**h2は1行**（全角26字まで）・**H1に副題を入れない**
- **表のセル内の棒`.hbar.cell`を部品に昇格**（2ページで再発明されていた）。これは図でなく数値の見せ方なので個別にfigureで包まず、**表全体を1つのfigureで包む**
- **チェックリストは2列の表**（項目 / 状態）。新部品は作らない
- **章ナビの項目数は4・6・8に寄せる**（5・7だと最終行の空きマスが「章の抜け」に見える）。lintはしない — 章の統合は判断が要る
- **`.section-lead`は定義しない**（`.sub-item p`と素の`p`で足りる）。ページ固有CSSで名前だけ増やさない

**lintに足した7項目**: `.badge.ink`が4個以上 / 01章のsh-subと結論本文の同文 / 章番号が01始まりでない / ドット式の小見出し番号 / h2が全角26字超 / H1の子要素 / **ページ固有`<style>`での共通部品の再定義・新変種**。最後のものは`.stat.win`・`.tag.geo`・`.steps-flow`のような「部品名を借りた別物」を止めるのが狙いで、`.card li`のような**中身の調整は対象外**にした（そこまで拾うと1ページ20件出て、本当の違反が埋もれる）。メディアクエリ内の調整と、template.cssを丸ごと`<style>`に埋め込む自己完結ページも対象外にしてある。

**機械適用の副作用**（9月4日に規約を24ページへ一斉適用した結果、資料側に出たもの）

- **凡例を「必ず置く」と書いたら、役割色6つの一覧が7回貼られた**。図には3役割しか出ていないのに、出ていない色まで凡例に並び、読者には「この図には何か足りない」と読める。規約は「置く/置かない」でなく**「何を載せるか」まで書かないと、機械は語彙をそのまま貼る**
- **3段階の格付けを`.badge`で表せと書いたら、中に`.badge.ink`（黒ベタ）が選ばれた**。語彙として`ink`があり「強調」としか書いていなかったため。黒ベタは焦がしベタより重く見えるので、高低が逆に読める。**色の語彙には「重さの順」まで書く**
- **「sh-subに答えの角度を1行」と書いたら、結論本文の1文目がそのまま複製された**。9ページで同文になっていた。**同じ場所に2回書かせない規約は、「何を書くか」を役割で分けて書く**

**保留**（他資料で再発したら再検討する）

- **◎○△×の文字色書式**: 評価記号に色を付ける形。1ページでしか使っておらず、色の語彙（緑=承認・紅=警告）と衝突する懸念がある
- **URL構造図の部品昇格**: いまは1ページのページ固有CSS。昇格の基準（複数ページで再発明されている・既存部品で代替できない）の①を満たさない

## 2026-09-04 — 結論は「01章」にする（箱をやめ、置き場を変えた）

同じ日の「専用部品`.conclusion-box`」（すぐ下の項）を撤回した決定。清野さんの判断。

**決めたこと**: ページの答えは**最初の章そのもの**にする。章ナビの1番目に「1 結論」、本文の最初の`.section`（`id="conclusion"`）のh2を「結論」にし、薄い大数字01+明朝見出しという章の文法で見せる。章の外に置く箱（`aside.conclusion` / `.conclusion-box`）は廃止。アラートの流用もしない。タブ式のページはタブ1を「結論」にする（開いた時に最初に見えるのがタブ1なので、30秒で答えに当たる目的は満たせる）。中身は答えの性格で選ぶ — 数字が答え=`.stat-row` / 判断を求める資料=`table.qa` / 一言で足りる=本文1段落。補足はその章の中の`ul.notes`に※で1〜2行。章の数は8が上限で、超えるページは答えに近い既存の章（概要・まとめ・比較）と統合する。章番号を振り直したら本文中の章参照も追従させ、`id`は変えない（他ページからのリンクを壊さないため）。

**なぜ箱をやめたか**: 「結論とその下が同じデザインの連続になる」という指摘に対し、箱の形を何度も変えて対処したが、根本は**答えを章の外に置いたこと**だった。章の文法は注記のアラートとも章ナビの格子とも形が違い、章ナビにも載る。先例も社内に2ページ（検索キーワード調査・ブログSEOの方針）あり、そちらは指摘が出ていなかった。

**分かったこと**: 同じデザインの連続は**色でなく形の問題**（すぐ下の項で得た教訓はここまでは正しかった）。ただしその先で、**形を変えるより置き場を変える方が根本的**だった。新しい部品を足す前に「既存の型に載せられないか」を先に見る。**結論の箱のために作ったCSSは結局ゼロになり**、`aside.conclusion`・`.conclusion-box`・`.c-note`・`p.ans`は1日で作って1日で消えた（箱をやめても残った`table.qa`と`.badge.muted`は下の「残したもの」のとおり使っている）。

**残したもの**: `table.qa`（問い→答えの表）と、章ナビの下余白の修正`.flow + .container{padding-top:0}`はそのまま使う。lintのチェックは「結論にアラートを流用」から「`aside.conclusion`・`.conclusion-box`が残っていたら指摘（結論は01章にする）」に差し替えた。

## 2026-09-04 — 結論はアラートの流用をやめ、専用部品にした

> **この項は同日中に撤回した**（上の「結論は「01章」にする」が現行の規約）。専用部品は作らず、答えは最初の章そのものにする。以下は撤回に至るまでの検討の記録。

清野さんの指摘「結論とその下が同じようなデザインの連続になる」から。原因は**ページの答えと、図の下の注記に同じ部品（アラート）を使っていた**こと。5ページでは緑＋藍が縦に並び、同じ形の箱が2つ続いていた。

**決めたこと**: 結論は専用の`.conclusion-box`（白面+1px罫+**左に焦がし3px**・見出しは小さな焦がしキッカー・答えは明朝16.5px）。補足（あわせて読む資料・この資料の読み方・進み具合）は**箱の中の最下段`.c-note`**に細い罫で分けて置き、外に2枚目のアラートを積まない。**アラートのデザインは変えない**（図の下の読み方ボックスなど本文中で正しく機能している）。lintに「結論にアラートを流用していないか」を追加。

**選定の経緯**（案を実物のページで作って見比べた）: 墨帯・左罫のみ・ラベル列・上罫の白面の4案 → 上罫の白面が近いが「上の2px墨罫が浮く」（すぐ上の章ナビも2px上罫の箱で、色違いの太罫が2本続くため）→ 線の扱いだけ4案 → **左に焦がし3px**を採用。棒を下まで通し補足も箱の中に入れる形で確定。

**分かったこと**: 「同じデザインの連続」は色でなく**形（シルエット）**の問題。同じ骨格のまま色を替えても族は変わらない。別物にしたいなら枠・面・囲みのどれかを変える。なお採用した`.conclusion-box`はアラートと骨格が近いままなので、**この形を注記側に増やさない**（アラートを4色目に増やさない規約と対で守る）。

**あわせて直した余白**: 章ナビの下余白48pxと`.container`の上余白36pxが足し算になり84pxの空白になっていた（全ページ共通）。`.flow + .container{padding-top:0}`で48pxに。結論の下は40pxを持たせ、章の地色が変わる境目に文字が貼り付かないようにした。

## 2026-09-04 — 他案件の資料から1件だけ採用（図の主張）

他案件の営業戦略資料を見て、いまの様式に無い型を3つ拾った。見本ページを組んで検討し、**1つだけ採用**した。

**採った: 図の主張（原則11）** — 図を`<figure class="fig" aria-label="…">`で包み、aria-labelに「読者に持ち帰ってほしい結論」を1文で書く。CSSは3行だけ。効き目は2つで、読み上げ対応と、**書けない図に気づけること**。9月3日のレビューで「物語のないドーナツ」4枚が削除判定になったが、この規約があれば描く前に気づける。lintでも機械チェックする（表のセル内の小さな棒は図でないので対象外）

**見送った2つと、その理由**

- **SVGで図を描く**（比率・桁違いの内訳を正確に見せる）: 見本を作る過程で見送りに転じた。**桁違いの内訳の例に選んだ図（サイトマップ419ページの内訳）は、レビュアーが「読者の判断に関わらないから削除」と判定した図そのものだった**。描けるようにしても、描くべきでない図が上手に描けるだけ。手数料比較も既存の`.bars`でほぼ同じことができる。代償として図の作り方が2系統になり、SVGは座標を手で調整するので保守が重く、エージェントに書かせると使いすぎる。ページ固有CSSを1476行→1240行に削った直後で方向が逆。**「これが描けなくて困った」が実際に起きたら再検討する**
- **層で掘る型**（口に出る言葉 → その裏にある事情 → 動き出すきっかけの3列）: CSSは8行と軽いが、**RINCLEで必要になった場面がゼロ**。174件のレビューでも要望は1件も出ていない。昇格の基準（複数ページで再発明されている・既存部品で代替できない）をどちらも満たさない。他案件で見て良さそうだった、だけでは入れない

**教訓**: 他案件の資料から型を借りる時は、**見本を自案件の実データで組んでみる**。組む過程で「その図はそもそも要らない」が見つかる。良さそうに見えた3件のうち2件がこれで落ちた。

## 2026-09-04 — ページ固有CSSの洗い出しから4部品を昇格

`documents/98_presentations/` の全24ページに残っていた「ページ固有CSS」を機械的に洗い出し、同じ組みが複数ページで別々に再発明されていたものを部品に昇格した。新設は **選択肢と採否 `.options`/`.option`** / **開発者向け詳細 `.dev-detail`+`.dd-body`** / **記号丸 `.num-chip`** / **APIメソッドバッジ `.method`** の4種。

**洗い出しのやり方**（再現できるように残す）

1. 各ページの `<style>` の中身を取り出し、コメントを落としてからクラス名（`\.([A-Za-z][\w-]*)`）を全部拾う
2. `template.css` と `parts.css` から同じ要領でクラス名を拾い、それを「既存部品」の集合とする
3. 1から2を引いた残り＝そのページが独自に足したCSS。クラスごとに「何ページで定義されているか」を数える
4. 2ページ以上で定義されているものを候補として、実物を見比べる

結果は **24ページ / 既存部品にないクラス423種**、うち2ページ以上で再発明されていたものが35種だった。数の多い順に `.warn`（6）・`.dd-body`/`.dev-detail`（4）・`.highlight`/`.method`/`.stat-card`/`.stat-grid`（3）と続く。

**昇格の基準**

- ①**複数ページで再発明されている**（同じ組みを各自が書き直している＝様式が割れる原因になっている）
- ②**既存部品で代替できない**（既存部品に寄せられるなら、部品を増やさず寄せる）

この2つを両方満たすものだけを昇格させた。**例外は `.options` の1件** — 定義は `stripe/payjp-vs-stripe.html` の `.routes-grid`/`.route-box` 1ページだけだが、「検討した案を並べて採否を示す」という情報の性格は提案資料に繰り返し現れ、既存の `.compare-box` は2択・良し悪しの部品なので3案以上と「沈める」が表せない。実証済みの型として昇格させた（清野さん判断）。

**昇格にあたって直したこと**（現物をそのまま入れない）

- `.option.dim`（不採用）は **`opacity` で沈めない**。現物は `opacity:.62` だったが、面ごと薄くなると紙の白が濁る。文字色と罫線をグレーに落とす形にした（既存の `tr.muted` と同じ思想）。この判断を原則3に明文化し、lintにも入れた
- **記号丸に案ごとの色を付けない**。現物は A=グレー・B=藍・C=焦がしだったが、藍は図解で「ユーザー」を指す役割色なので意味が二重になる。採否は**枠とバッジ**の2つが示す。同じ理由で `.num-chip` の色の変種は `.brand`（焦がし輪郭）だけにし、現物にあった `.blue`/`.green` は作らなかった
- **結論バッジは既存の語彙で表す**。現物は `.route-verdict.yes/.no/.neutral` という独自の札だったが、採用=`.badge.good` / 対抗案=`.badge.plain` / 不採用=`.badge.muted` で足りる。新しい色を作らない
- `.dev-detail` は4ページで**微妙に定義が違った**（`margin-top:18px` と `margin:18px 0`・`.dd-body` 内の見出しや表の調整の有無）ので、いちばん整った形に揃えて統合した。**クラス名は現行のまま**にした — 改名すると4ページ分の置き換えが増えるだけで、得るものがない
- `.method` は3ページでほぼ同じものが3通りに書かれていた。v1ページだけ `.method-get` のハイフン記法だったので **ドット記法 `.method.get` に統一**。`.put`（琥珀）を足し、`--gold-line:#EAD9AE` をトークンに追加した（`--ok-line`/`--info-line`/`--danger-line` と揃える。各ページが `#EAD9AE` を直書きしていた）

**「新設しない」と判断したもの**（既存部品に寄せる）

- **`.stat-card`/`.stat-grid`（3ページ）→ `.stat` / `.stat-row`**。ラベル+大きな数字という同じ組みで、既存部品がそのまま使える。独自に作ると「ラベルが上」の規約（2026-08-26）から外れる個体が生まれる
- **`.tier-badge`（2ページ）→ `.badge`**。格付けは `.badge.brand`/`.ink`/`.plain` の3段で足りる（2026-09-04 #7 で「5点ドットを新設しない」と決めたのと同じ理屈）
- **`.checklist`（2ページ）→ `ol.steps`**。中身は「✓/✗のアイコンを左に置いた箇条書き」で、読者が実行する手順なら `ol.steps` が正、○×の判定を見せたいなら `.compare-box.good/.bad` が正。どちらの用途かで既存部品が決まるので、中間の部品は要らない
- **`.warn`（6ページ）→ 新設しない**。琥珀のバッジ・アラートは語彙外（琥珀は「お金」の役割色）。課題・やり残しは `ul.notes` の※か `.compare-box.bad` へ（2026-09-04 #3の続き）
- **`tr.highlight`/`tr.hl`/`tr.sum`（4ページ）→ 新設しない**。色ベタの行ハイライトは規約違反で、強調は焦がしの太字か `td.std` の薄い面

**lintに足した3項目**: 色ベタの行ハイライト（`tr.xxx td{background}`）/ 琥珀のバッジ（`.badge` 系のルールが `--gold` 等を使う）/ 沈めるのに `opacity`。いずれも**ページ固有の `<style>` を読んで判定する**。CSSを丸ごと `<style>` に埋め込む自己完結ページで誤検知しないよう、opacityは **template.css 自身がopacityを使うセレクタのクラス名（`.recap`/`.funnel`/`.rv` 等）を許可リストにして除外**した（旧配色チェックが template.css の色を自動許可しているのと同じ作り）。導入前に24ページへかけて、拾ったのは実在の違反5件だけ・誤検知0件であることを確認している。

## 2026-09-04 — 初見レビュー174件から規約化した8件

`documents/98_presentations/` の全24ページを、資料を初めて見る3本のエージェントに1280px幅のスクリーンショットで評価させ、指摘174件を回収（記録: `documents/98_presentations/review/2026-09-03/summary.md`）。うち168件は既存の部品で直せるもので、規約変更・新部品が要るのは8件だった。清野さんの判断で6件を採用・2件を不採用とした。

**採った6件**

- **#1 footerは「正本: <mdパス>」だけにする**（原則5を改定）。「（md更新時はこのHTMLも同期すること）」は保守者向けの指示で、読者には意味がない。footer直前のHTMLコメントに移した。同じ理屈で、タスク番号（#44等）・スクリプト名・更新の経緯も本文でなくコメントに置く。**footer書式が3種に割れていた**のが発端で、lintに「footer本文に『同期』が入っていたら指摘」を追加（実在の5ページを検出した）
- **#2 章ナビの直下・最初の章の前に「結論」を置く**（ページ構造に追加）。
  > **この項は同日中に改定した**（現行は「結論は01章」＝このファイル最上段の項）。章の外の箱（`<aside class="conclusion">`）も、短い答えを`.alert.alert-green`で置く形も、いまは使わない。以下は改定前の記録。
  24ページ中19ページで「この資料の答え」が最初の画面になく、末尾の墨帯や5章目にあった。**短い答え**は`.alert.alert-green`+「結論:」1〜2文、**判断を求める資料**は「問い→答え」の表`table.qa`（stripe/receipt-invoice §00 の型）。3班の提案が「alert 1行」と「3問3答の表」に割れたが、どちらか一方に決めず**答えの長さで使い分ける**形にした。
  実装上の落とし穴: `.container`直下に`div`で置くと `.section:nth-of-type(odd)`（章の白⇄紙の交互）が1つずれる。そのため **`<aside class="conclusion">`** で置くことを規約にした（`aside`は`div`と別のタグなので`nth-of-type`の数に入らない）
- **#3 alertの色の意味を固定**: 緑=結論・決定 / 藍=読み方・補足 / 紅=リスク・注意。**琥珀（黄）のalertは採用しない** — 琥珀は役割色で「お金」に割り当て済みで、alertにも使うと意味が二重になる。実ページには`alert-amber`（36箇所）と`alert-brand`（10箇所）が生えており、色が意味を運ばなくなっていた。「課題・やり残し」は`ul.notes`の※か`.compare-box.bad`に置く（課題＝いま悪い / リスク＝これから悪くなる、で部品が分かれる）。lintは`alert-blue/green/red`以外の変種を指摘する
- **#4 変更点バッジの語彙を4語に固定**: 「決定済み」（緑=`.badge.good`）/「弊社提案」（焦がし=`.badge.brand`）/「変更点」（焦がし=`.badge.brand`）/「旧設計」（グレー=新設の`.badge.muted`）。同じ意味の語が資料ごとに割れると、読者は色と語の対応を毎回覚え直すことになる。`CHANGED`等の英字は使わない（実ページに6箇所）。lintは英字の状態語（changed/new/old/updated 等）をバッジ内に見つけたら指摘する
- **#5 幅1100px以下でH1を44pxに落とすメディアクエリ**を追加。**見出しの階段4段（52 / 30 / 19 / 16px）は維持**し、落とすのはページ題のH1だけ。SPAシェルのiframe内では本文幅が約1020pxになり、52pxのままだと題が2行に折れて章ナビに食い込んでいた
- **#6 shell.htmlのようこそ画面を入口ページに拡張**。「資料を選択してください」の1行だけで、何の資料集か・どれから読むかが分からなかった。**資料集の一言 → まず読む3本（章ナビと同じ罫線グリッド）→ セクションごとの1行説明**の3段構成にし、文言は`welcome`・`sectionNotes`に書くだけ・タイトルは`pages`配列から引く形にした（資料が増えても書き分けが割れない）。あわせて`AUTO_OPEN_FIRST`の既定を`false`にした — ようこそ画面を作っても、既定で先頭ページが開いてしまえば誰も見ない

**採らなかった2件と、その代替**

- **#7 セル内の5点ドット表示（●●●○○）は新設しない**。評価スケールの見せ方が5通り以上に割れていた問題は本物だが、量の比較は既存の`.hbar`（主役でない棒は`.fill.dim`で沈める）で読め、3段階の格付けは`.badge.brand/.ink/.plain`で足りる。**部品を増やすほど「どれを使うか」の判断が増え、割れの原因が増える**
- **#8 「決めることリスト」部品（決めること / 誰が / いつ）は新設しない**。3列の表そのもので、既存の`table`（項目名=`td.label`）で組める。判断を求める資料の入口は#2の`table.qa`が担う

**やり方の教訓**: 初見レビューは「規約の穴」を見つけるのに効く（作った本人は自分の規約の中で考えるので、規約そのものの欠落は見えない）。ただし**レビュアーの提案をそのまま部品にしない** — 174件のうち規約変更が要ったのは8件、実際に新設したCSSは`.conclusion`・`table.qa`・`.badge.muted`の3つで、**そのうち`.conclusion`は同日中の改定で消えたので、残ったのは`table.qa`と`.badge.muted`の2つ**だった。既存部品で直せるかを先に確かめる。

## 2026-09-02 — 初見レビューからの規約追加

月次レポートを初見のエージェントにレビューさせ、作った本人には見えない指摘を17件回収。規約化したもの:

- **率の前月比はpt差・順位は位の差で表示**（%の%変化は直感的でない）。差は表示値（丸め後）どうしで計算 — 生値の差だと「22%−35%なのに-14pt」になり検算で引っかかる
- **単位が混在するファネルは、小さい単位の段を縮小スケールにして差を残す**。上限は「直上の段の8割」— 素朴に直すと下の段が上より広くなりファネルの形が崩れる（snippets.htmlの雛形コメントにも記載）
- 内訳の合計が全体と一致しない集計（GA4の流入元等）は「誤りではない」の注記を必ず付ける — 検算する読者は必ず引っかかる
- 初見レビューは「作った本人の盲点」に効くが、毎月やるものではない（1回約19万トークン。構造系の指摘は一度直せば再発しない）。呼びどきは「構成を大きく変えた時・新しい種類の資料の初回・大きな主張を載せる時」の3つ。定常月はlint+機械検算で足りる

## 2026-09-02 — 月次レポートの実戦から部品を昇格

RINCLEの月次レポート作りで発明して価値が実証されたものをスキルに取り込んだ:

- **ファネルの前月比較（ゴーストバー）**: 当月バーの真下に前月量の淡色細バー+率に「（前月 n%）」併記。「量は減ったが通過率が改善」のような交差の物語が図だけで伝わる
- **時間の色を役割色と同格の規約に**: 濃=当月（焦がし）/ 淡=前月（下地#EAE2D6）。barsに暗黙だったものを明文化
- **読み方ボックス**: 複雑な図の直下にalert-blue+「この図の読み方:」を1個。※注記に長文の解釈を書かない
- **`.term-row`**: 用語2〜3個の横並び（毎回inline flexを書いていた）
- **`.label-sub`**: 表の主ラベルに原語を薄く併記（GA4のチャネル名の日本語化で発明）
- **ファネル・ラベル列型`.funnel.labeled`**: 段名が長いとバーに収まらず注釈色に落ちる問題の根本解決
- **「情報の性格→部品の選択」表を新設**: ※が1つの表に14個並んだ反省。用語=term/図の読み方=読み方ボックス/但し書き=※（3個まで目安）の交通整理
- **lintに`<!-- lint-mode: mockup -->`**: UI模写ページの描写用直書き（影・固定幅）を免除
- **地域タイルマップ`.tilemap`新設**: 「地図はコスト高」と一度見送ったが、県境を描かない正方形タイル方式なら自己完結で描けると判明。面の広がり（関西一円の濃さ・店舗空白県の需要）が横棒では出せない示唆を生む。地図単体では大小が読めないため、上位の横棒併置を規約にした
- **`.chart.compact`新設**: ドーナツの凡例min-width:280pxが半分幅の置き場で下に折り返した（月次レポートのデバイス欄）。半幅用の変種として正式化
- **recapは所見のあるページに置かない**: レポート末尾に足したら所見と要約が二度手前になり撤去。選択表に明文化
- **100%積み上げ構成バー`.stackbar`新設・チャート個数の緩和**: 月次レポートの「検索表示の75%はノイズ」を表でなく帯で見せるため。「1ページ1〜2個」は読み物向けの目安とし、数字が主役のレポートは章ごと1〜2個まで可とした
- **表の2段ヘッダー`th.th-grp`+`tr.muted`**: Excelの体制表（「体制」でPM/Eng列を束ねる）を見た清野さんの要望から。色ベタハイライトの代わりは焦がし太字か`td.std`

## 2026-09-02 — SKILL.mdの経緯分離・検証節の参照化

- SKILL.mdが16KBに肥大し、原則6・10・11が「規約+決めた経緯」の混在長文になっていたため、経緯をこのファイルに分離。SKILL.mdは規約だけを保持し、経緯は「→decisions.md 日付」で参照する
- SKILL.md検証節のチェック項目列挙がlint.py docstringとずれていた（文章・敬語チェックの追加が検証節に未反映）。二重管理をやめ、**チェック一覧はlint.py docstringだけが正**とした

## 2026-09-01 — 文章・敬語規約、料金数字の誤読、表の字下げ

- **文章規約**（全角括弧・半角スラッシュ+前後スペース・接続助詞でつながない）と**敬語規約**（主体で使い分け・「で結構です」「ご〜される」禁止）を追加し、機械判定できる分をlint.pyに実装。規約2件がlintに入っていない漏れが同日発覚し、5点セットの掟に「lint.pyも揃える」を明記
- **`.stat`の中サイズ`.n.md`（30px）新設**: 料金提案の初見レビューで、単価（答えでない数字）を月額（答えの数字）と同じ特大で見せたら「固定料金」と誤読された。数字の大きさで序列をつける
- **表の字下げは`td.label.ind`に統一**: 全角空白の直書きは検索・コピーに混ざり、折り返しで字下げが消える（SEOレポートの旧方式から移行）。長い例示は`td .td-sub`で行内に落とす（ヒアリング11項目表で1行1項目に戻した実績）

## 2026-08-28 — SPAシェルの雛形化

- RINCLE `documents/98_presentations/index.html` を汎用雛形 `shell.html` に昇格。iframe内リンク追従のバグは雛形側で修正済み

## 2026-08-26 — 並列生成の参照割れと5点セットの掟

- 複数エージェントに9ページを並列生成させたところ、構造検証が全部通っているのに **キッカー文言3種・recap配置9ページ・stat順18箇所の参照割れ**が起きていた。原因の一つはスキルのsnippetsに残っていた旧順（数字→ラベル）の見本1つ。教訓:
  - **statの順序は全変種で「ラベルが上」に統一**（例外を1つでも残すと、それが参照されて割れる）
  - 生成を並列分担させた後は必ずlint.pyを全ファイルにかける
  - 総点検で7部品分の雛形漏れも発覚 → 「5点セットで更新する」を掟として明文化

## 2026-08-25 — リード文の幅・フロー図の使い分け・ノードの色

- **ヒーローのリード文の680px幅制限を廃止**: 中途半端な位置で改行され、かえって読みにくかった。部品に個別max-widthを付けない原則の一部（例外は投影用`.slide`の額のみ）
- **フロー図の使い分けを決定**: 複数役割のやり取り=レーン格子`.lanes`（役割間のタイミングが格子で揃う。固定px幅の空白divによる位置合わせは崩れの元なので禁止）/ほぼ単役割=箱矢印`.swim`（レーン型だと1行だけの表になって間延びする）
- **`.swim`ノードの部分的な色バーを廃止**: 役割は行ラベルが示すので、ノードはニュートラルなピル型チップに統一

## 2026-08-21 — 誌面エディトリアル様式へ全面改定

- 旧パステル様式（#F95320系の原色・角丸カード・ピル型タブ）から全面改定。RINCLE案件で確立
- **章の背景は白⇄紙の交互**: 縦資料は余白だけだと章の切り替えが分かりにくい
- ブランド原色は使わず「焦がしトーン」に落とす（RINCLE: #F95320 → #B8532F）
- 当初の見た目の正本はClaude Designキャンバス「RINCLE デザインシステム」だったが、後にgallery.htmlへ移行（キャンバスの元データは `_source/` に断面として保存。**以後更新されない参考資料**）
